# netscope – Konfiguration
# ─────────────────────────────────────────────────────
# Port auf dem netscope erreichbar ist
NETSCOPE_PORT=8080

# Verzeichnis auf dem Host mit den Daten-Dateien:
#   index.html               (das Template)
#   network_devices.json     (wird vom Scan überschrieben)
#   docker_containers.json   (wird vom Scan überschrieben)
#
# Standard: ./data (relativ zum docker-compose.yml)
NETSCOPE_DATA_DIR=./data
